---
// src/pages/bear/index.astro
import Base from "@/layouts/Base.astro";
import { getCollection } from "astro:content";
import dateFormat from "@/lib/utils/dateFormat";

const posts = await getCollection("posts", ({ data }) => !data.draft);
const sortedPosts = posts.sort((a, b) => b.data.date.getTime() - a.data.date.getTime());

const pillars = [
  { id: 'macro-liquidity', name: 'Macro Liquidity', icon: '💧' },
  { id: 'systemic-risk', name: 'Systemic Risk', icon: '⚠️' },
  { id: 'fraud-red-flags', name: 'Fraud & Red Flags', icon: '🚩' },
  { id: 'bearish-frameworks', name: 'Bearish Frameworks', icon: '📉' },
  { id: 'crowd-psychology', name: 'Crowd Psychology', icon: '🧠' },
  { id: 'capital-preservation', name: 'Capital Preservation', icon: '🛡️' }
];
---

<Base title="Txchyon Bear // Defensive Market Intelligence">
  <div class="bear-home min-h-screen bg-[#0f1113] text-[#e0e0e0]">
    <!-- Hero Section -->
    <section class="py-20 border-b border-[#1f2226]">
      <div class="container mx-auto px-4 text-center">
        <h1 class="text-5xl md:text-7xl font-black uppercase italic tracking-tighter mb-4 text-white">
          Txchyon <span class="text-red-600">Bear</span>
        </h1>
        <p class="text-lg md:text-xl font-bold text-gray-400 tracking-widest uppercase mb-12 max-w-2xl mx-auto">
          Defensive Intelligence Division. Data-driven research for high-risk regimes.
        </p>
        
        <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
          {pillars.map(pillar => (
            <a href={`/bear/blog/${pillar.id}`} class="p-6 rounded-2xl bg-[#1a1d21] border border-[#2d3136] hover:border-red-600 transition-all group">
              <span class="text-3xl mb-3 block">{pillar.icon}</span>
              <span class="text-[10px] font-black uppercase tracking-widest group-hover:text-red-500 transition-colors">{pillar.name}</span>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- Post Grid -->
    <section class="py-20">
      <div class="container mx-auto px-4">
        <div class="flex items-center justify-between mb-12">
          <h2 class="text-2xl font-black uppercase italic tracking-tighter text-white">Recent Intelligence</h2>
          <div class="h-px flex-1 bg-[#1f2226] mx-8"></div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          {sortedPosts.map(post => (
            <a href={`/bear/blog/${post.data.pillar}/${post.data.subcategory}/${post.slug.split('/').pop()}`} class="group">
              <div class="relative aspect-video rounded-2xl overflow-hidden mb-6 border border-[#1f2226]">
                <img src={post.data.image} alt={post.data.title} class="object-cover w-full h-full transition-transform group-hover:scale-105" />
                <div class="absolute top-4 left-4">
                  <span class="px-3 py-1 bg-red-600 text-white text-[9px] font-black uppercase tracking-widest rounded-full">
                    {post.data.pillar}
                  </span>
                </div>
              </div>
              <h3 class="text-xl font-bold mb-3 group-hover:text-red-500 transition-colors">{post.data.title}</h3>
              <p class="text-sm text-gray-500 line-clamp-2 mb-4">{post.data.description}</p>
              <div class="flex items-center text-[10px] font-black uppercase tracking-widest text-gray-600">
                <span>By {post.data.author}</span>
                <span class="mx-2">•</span>
                <span>{dateFormat(post.data.date)}</span>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  </div>
</Base>

<style>
  .bear-home {
    background-color: #0f1113;
  }
</style>
